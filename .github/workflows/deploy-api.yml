name: Deploy API

# Railway handles API deploys natively via its GitHub integration.
# It watches apps/api, packages/engine, and Dockerfile.api and auto-deploys
# to production on push to master. No workflow needed here.
#
# This file is kept as a placeholder. If Railway integration ever breaks,
# the Fly.io fallback below can be re-enabled:
#
# on:
#   push:
#     branches: [master]
#     paths:
#       - 'apps/api/**'
#       - 'packages/engine/**'
#       - 'Dockerfile.api'
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: superfly/flyctl-actions/setup-flyctl@master
#       - run: flyctl deploy --config fly.toml --dockerfile Dockerfile.api
#         env:
#           FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

"""Data-layer utilities for the PrepRank engine.

Hosts data-hygiene infrastructure (school name resolution, OOS-flag
correction, etc.) that operates above the ingest layer but below the
feature/prediction layers.
"""
